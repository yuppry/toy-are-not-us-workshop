import { ProductGenderPipe } from './product-gender.pipe';

describe('ProductGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
